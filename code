=======================================
INICIO FICHERO >> ./mfe-shell/index.html
=======================================
// HTML principal para MFE Shell
<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>MFE Shell</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.jsx"></script>
  </body>
</html>
=======================================
FIN FICHERO >> ./mfe-shell/index.html
=======================================

=======================================
INICIO FICHERO >> ./mfe-shell/src/main.jsx
=======================================
// Componente raíz para MFE Shell con verificación de conexión backend
import React, { useEffect, useState } from 'react';
import { createRoot } from 'react-dom/client';

function App() {
  const [backendStatus, setBackendStatus] = useState('Comprobando conexión...');

  useEffect(() => {
    fetch('/api/health')
      .then(res => res.text())
      .then(data => setBackendStatus(`✅ Backend responde: ${data}`))
      .catch(err => setBackendStatus(`❌ Error al conectar: ${err.message}`));
  }, []);

  return (
    <div style={{ fontFamily: 'sans-serif', padding: 20 }}>
      <h1>MFE Shell</h1>
      <p>Micro Frontend principal funcionando 🎯</p>
      <p>{backendStatus}</p>
    </div>
  );
}

createRoot(document.getElementById('root')).render(<App />);
=======================================
FIN FICHERO >> ./mfe-shell/src/main.jsx
=======================================

=======================================
INICIO FICHERO >> ./mfe-login/index.html
=======================================
// HTML principal para MFE Login
<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>MFE Login</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.jsx"></script>
  </body>
</html>
=======================================
FIN FICHERO >> ./mfe-login/index.html
=======================================

=======================================
INICIO FICHERO >> ./mfe-login/src/main.jsx
=======================================
// Componente raíz para MFE Login con verificación de conexión backend
import React, { useEffect, useState } from 'react';
import { createRoot } from 'react-dom/client';

function App() {
  const [backendStatus, setBackendStatus] = useState('Comprobando conexión...');

  useEffect(() => {
    fetch('/api/health')
      .then(res => res.text())
      .then(data => setBackendStatus(`✅ Backend responde: ${data}`))
      .catch(err => setBackendStatus(`❌ Error al conectar: ${err.message}`));
  }, []);

  return (
    <div style={{ fontFamily: 'sans-serif', padding: 20 }}>
      <h1>MFE Login</h1>
      <p>Formulario de autenticación listo 🚀</p>
      <p>{backendStatus}</p>
    </div>
  );
}

createRoot(document.getElementById('root')).render(<App />);
=======================================
FIN FICHERO >> ./mfe-login/src/main.jsx
=======================================

=======================================
INICIO FICHERO >> ./mfe-dashboard/index.html
=======================================
// HTML principal para MFE Dashboard
<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>MFE Dashboard</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.jsx"></script>
  </body>
</html>
=======================================
FIN FICHERO >> ./mfe-dashboard/index.html
=======================================

=======================================
INICIO FICHERO >> ./mfe-dashboard/src/main.jsx
=======================================
// Componente raíz para MFE Dashboard con verificación de conexión backend
import React, { useEffect, useState } from 'react';
import { createRoot } from 'react-dom/client';

function App() {
  const [backendStatus, setBackendStatus] = useState('Comprobando conexión...');

  useEffect(() => {
    fetch('/api/health')
      .then(res => res.text())
      .then(data => setBackendStatus(`✅ Backend responde: ${data}`))
      .catch(err => setBackendStatus(`❌ Error al conectar: ${err.message}`));
  }, []);

  return (
    <div style={{ fontFamily: 'sans-serif', padding: 20 }}>
      <h1>MFE Dashboard</h1>
      <p>Panel de control funcionando 📊</p>
      <p>{backendStatus}</p>
    </div>
  );
}

createRoot(document.getElementById('root')).render(<App />);
=======================================
FIN FICHERO >> ./mfe-dashboard/src/main.jsx
=======================================
